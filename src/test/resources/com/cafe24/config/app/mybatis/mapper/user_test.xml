<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<insert id="insert" parameterType="uservo">
	<![CDATA[
		insert into user(no, id, name, password, pw_question, pw_answer, phone_number, email, birth_date, role, savings, end_date, gender) 
		values(null, 
		AES_ENCRYPT(#{id}, SHA2("aaa", 512)), 
		AES_ENCRYPT(#{name}, SHA2("aaa", 512)), 
		SHA2(#{id},512), 
		#{passwordQuestion}, 
		AES_ENCRYPT(#{passwordAnswer},SHA2("aaa", 512)), 
		AES_ENCRYPT(#{phoneNumber}, SHA2("aaa", 512)), 
		AES_ENCRYPT(#{email},SHA2("aaa", 512)), 
		#{birthDate}, 'U', #{savings}, #{endDate}, #{gender});
	]]>
	
	<selectKey keyProperty="no" resultType="long" order="AFTER">
	<![CDATA[
		select last_insert_id()
	]]>
	</selectKey>
	</insert> 
	
</mapper>
